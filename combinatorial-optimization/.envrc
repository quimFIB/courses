# This project's own uv environment. direnv switches to it on entering the
# directory (shell and Emacs via envrc) and restores ~/.python-env on leaving.
export VIRTUAL_ENV="$PWD/.venv"
PATH_add .venv/bin
