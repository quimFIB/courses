{
  "title": "Combinatorial & constrained optimization",
  "build": "slides/site/build.py",
  "blurb": "32 units in which theory is interleaved with from-scratch Python implementations, each then benchmarked against the industrial solver that does the same job.",
  "needs": "uv sync — a virtualenv and a solver stack",
  "site_is": "theory only; the labs are in the repository"
}
