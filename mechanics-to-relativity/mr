#!/usr/bin/env python3
"""Entry point: ./mr <command>. Standard library only — there is nothing to install."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
from mrlib.cli import main  # noqa: E402

main()
